CADBase Backend Processes
=====
Processes downloaded data and deletes files from the storage for CADBase

This crate for parsing new files and delete files with `is_delete` flag in storage

## TODO:
  - [ ] make setting mime types for files
